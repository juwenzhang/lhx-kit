services:
  api:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    depends_on: []
    restart: unless-stopped

  # lhx:db-services
  # (db-pg / db-mysql features append here)

  # lhx:cache-services
  # (cache-redis feature appends here)

volumes: {}
