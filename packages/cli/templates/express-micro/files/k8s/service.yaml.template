apiVersion: v1
kind: Service
metadata:
  name: <%= packageName %>
  labels:
    app: <%= packageName %>
spec:
  selector:
    app: <%= packageName %>
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
