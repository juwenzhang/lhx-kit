apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastify-micro-smoke
  labels:
    app: fastify-micro-smoke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastify-micro-smoke
  template:
    metadata:
      labels:
        app: fastify-micro-smoke
    spec:
      containers:
        - name: fastify-micro-smoke
          image: fastify-micro-smoke:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
          livenessProbe:
            httpGet:
              path: /livez
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
