apiVersion: v1
kind: Service
metadata:
  name: koa-micro-smoke
  labels:
    app: koa-micro-smoke
spec:
  selector:
    app: koa-micro-smoke
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
