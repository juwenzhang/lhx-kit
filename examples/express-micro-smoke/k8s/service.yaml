apiVersion: v1
kind: Service
metadata:
  name: express-micro-smoke
  labels:
    app: express-micro-smoke
spec:
  selector:
    app: express-micro-smoke
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
