apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-micro-smoke
  labels:
    app: express-micro-smoke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express-micro-smoke
  template:
    metadata:
      labels:
        app: express-micro-smoke
    spec:
      containers:
        - name: express-micro-smoke
          image: express-micro-smoke:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
          livenessProbe:
            httpGet:
              path: /livez
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
